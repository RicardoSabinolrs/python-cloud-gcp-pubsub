apiVersion: v1
kind: Service
metadata:
  name: {{ include "google-cloud-pubsub.fullname" . }}
  labels:
    {{- include "google-cloud-pubsub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "google-cloud-pubsub.selectorLabels" . | nindent 4 }}
