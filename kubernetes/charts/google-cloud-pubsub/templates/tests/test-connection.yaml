apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "python-google-cloud-pubsub.fullname" . }}-test-connection"
  labels:
    {{- include "python-google-cloud-pubsub.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "python-google-cloud-pubsub.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
